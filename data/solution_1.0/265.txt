Trace(cost=6):
0
  ParseGridTask()
  IndependentParse(x_mode=ParseMode.crop,y_mode=ParseMode.crop,x_bg_model=return 0,y_bg_model=return 0,unknown_background=False,x_partition_color=-1,y_partition_color=-1,y_constant_size=(5, 3))
1
  FreeDrawTask()
  FreeDraw(param=FreeDrawParam.normal,width_model=return 5,height_model=return 3,pixel_model=if -diagonal(x,y) >= 1:
  return -1
elif cell(x+1,y+1) >= 1:
  return 3
elif cell(x-1,y-1) >= 0:
  return 7
elif cell(x,y-1) >= -1:
  return 8
else:
  return 6)
2
  DrawCanvasTask()
  DrawCanvas(width_model=return 5,height_model=return 3,layer_model=None,is_all_equal=False)