Trace(cost=4):
0
  ParseGridTask()
  IndependentParse(x_mode=ParseMode.crop,y_mode=ParseMode.proximity_diag,x_bg_model=return 0,y_bg_model=return 0,unknown_background=False,x_partition_color=-1,y_partition_color=-1,y_constant_size=(1, 1))
1
  FreeDrawTask()
  FreeDraw(param=FreeDrawParam.normal,width_model=return 1,height_model=return 1,pixel_model=if cell(x,y) == 3:
  return 2
elif -shape.top_color >= -1:
  return 4
elif grid_width >= 14:
  return 6
else:
  return 8)
2
  CropTask(crop_from_out_shapes=True)
  Crop(bound_model=DummyVisualModel(inner_models=[return 0, return 0, return 1, return 1]))
3
  DrawCanvasTask()
  DrawCanvas(width_model=return 1,height_model=return 1,layer_model=None,is_all_equal=False)