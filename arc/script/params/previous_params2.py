from ...constant import *

previous_v1_params = {}

previous_v1_params[108] = GlobalParams(
    parser_x_modes=(ParseMode.partition,),
    parser_y_modes=(ParseMode.crop,), max_reparse=0)

previous_v1_params[109] = GlobalParams(
    parser_x_modes=(ParseMode.proximity_diag,),
    parser_y_modes=(ParseMode.proximity_diag,), max_reparse=0)

previous_v1_params[111] = GlobalParams(
    parser_x_modes=(ParseMode.proximity_diag,),
    parser_y_modes=(ParseMode.proximity_diag,), max_reparse=0)

previous_v1_params[112] = GlobalParams(
    parser_x_modes=(ParseMode.proximity_diag,),
    parser_y_modes=(ParseMode.crop,), max_reparse=0)

previous_v1_params[113] = GlobalParams(
    parser_x_modes=(ParseMode.proximity_diag,),
    parser_y_modes=(ParseMode.proximity_diag,), max_reparse=0)

previous_v1_params[115] = GlobalParams(
    parser_x_modes=(ParseMode.crop,),
    parser_y_modes=(ParseMode.crop,), max_reparse=0)

previous_v1_params[124] = GlobalParams(
    parser_x_modes=(ParseMode.crop,),
    parser_y_modes=(ParseMode.crop,), max_reparse=0)

previous_v1_params[125] = GlobalParams(
    parser_x_modes=(ParseMode.color_proximity_diag,),
    parser_y_modes=(ParseMode.color_proximity_diag,), max_reparse=0)

previous_v1_params[126] = GlobalParams(
    parser_x_modes=(ParseMode.proximity_diag,),
    parser_y_modes=(ParseMode.color_proximity_diag,), max_reparse=0)

previous_v1_params[127] = GlobalParams(
    parser_x_modes=(ParseMode.proximity_diag,),
    parser_y_modes=(ParseMode.proximity_diag,), max_reparse=0)

previous_v1_params[128] = GlobalParams(
    parser_x_modes=(ParseMode.proximity_diag,),
    parser_y_modes=(ParseMode.proximity_diag,), max_reparse=0)

previous_v1_params[131] = GlobalParams(
    parser_x_modes=(ParseMode.proximity_diag,),
    parser_y_modes=(ParseMode.proximity_diag,), max_reparse=0)

previous_v1_params[134] = GlobalParams(
    parser_x_modes=(ParseMode.proximity_diag,),
    parser_y_modes=(ParseMode.proximity_diag,), max_reparse=0)

previous_v1_params[138] = GlobalParams(
    parser_x_modes=(ParseMode.proximity_diag,),
    parser_y_modes=(ParseMode.proximity_diag,), max_reparse=0)

previous_v1_params[139] = GlobalParams(
    parser_x_modes=(ParseMode.crop,),
    parser_y_modes=(ParseMode.crop,), max_reparse=0)

previous_v1_params[141] = GlobalParams(
    parser_x_modes=(ParseMode.proximity_diag,),
    parser_y_modes=(ParseMode.proximity_diag,), max_reparse=0)

previous_v1_params[145] = GlobalParams(
    parser_x_modes=(ParseMode.proximity_diag,),
    parser_y_modes=(ParseMode.proximity_diag,), max_reparse=0)

previous_v1_params[146] = GlobalParams(
    parser_x_modes=(ParseMode.proximity_normal,),
    parser_y_modes=(ParseMode.proximity_normal,), max_reparse=0)

previous_v1_params[149] = GlobalParams(
    parser_x_modes=(ParseMode.crop,),
    parser_y_modes=(ParseMode.crop,), max_reparse=0)

previous_v1_params[151] = GlobalParams(
    parser_x_modes=(ParseMode.proximity_diag,),
    parser_y_modes=(ParseMode.proximity_diag,), max_reparse=0)

previous_v1_params[154] = GlobalParams(
    parser_x_modes=(ParseMode.proximity_diag,),
    parser_y_modes=(ParseMode.proximity_diag,), max_reparse=0)

# TODO 155 is a problem

previous_v1_params[163] = GlobalParams(
    parser_x_modes=(ParseMode.crop,),
    parser_y_modes=(ParseMode.crop,), max_reparse=0)

previous_v1_params[165] = GlobalParams(
    parser_x_modes=(ParseMode.proximity_diag,),
    parser_y_modes=(ParseMode.proximity_diag,), max_reparse=0)

previous_v1_params[168] = GlobalParams(
    parser_x_modes=(ParseMode.proximity_diag,),
    parser_y_modes=(ParseMode.proximity_diag,), max_reparse=0)

previous_v1_params[170] = GlobalParams(
    parser_x_modes=(ParseMode.proximity_diag,),
    parser_y_modes=(ParseMode.proximity_diag,), max_reparse=0)

previous_v1_params[171] = GlobalParams(
    parser_x_modes=(ParseMode.proximity_diag,),
    parser_y_modes=(ParseMode.proximity_diag,), max_reparse=0)

previous_v1_params[178] = GlobalParams(
    parser_x_modes=(ParseMode.crop,),
    parser_y_modes=(ParseMode.crop,), max_reparse=0)

previous_v1_params[185] = GlobalParams(
    parser_x_modes=(ParseMode.crop,),
    parser_y_modes=(ParseMode.proximity_diag,), max_reparse=1)

previous_v1_params[186] = GlobalParams(
    parser_x_modes=(ParseMode.proximity_diag,),
    parser_y_modes=(ParseMode.proximity_diag,), max_reparse=0)

previous_v1_params[193] = GlobalParams(
    parser_x_modes=(ParseMode.crop,),
    parser_y_modes=(ParseMode.crop,), max_reparse=0)

previous_v1_params[195] = GlobalParams(
    parser_x_modes=(ParseMode.proximity_diag,),
    parser_y_modes=(ParseMode.proximity_diag,), max_reparse=0)