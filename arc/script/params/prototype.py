from ...constant import *

proto = GlobalParams(enable_free_draw=False,
                     enable_edge=False,
                     enable_merge=False,
                     enable_stack=False,
                     enable_split=False)
