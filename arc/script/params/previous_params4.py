from .prototype import *

previous_v1_params4 = {}

previous_v1_params4[301] = proto.update(
    parser_x_modes=(ParseMode.proximity_diag,),
    parser_y_modes=(ParseMode.proximity_diag,))

previous_v1_params4[302] = proto.update(
    parser_x_modes=(ParseMode.crop,),
    parser_y_modes=(ParseMode.proximity_diag,))

previous_v1_params4[303] = proto.update(
    parser_x_modes=(ParseMode.crop,),
    parser_y_modes=(ParseMode.crop,), enable_free_draw=True)

previous_v1_params4[304] = proto.update(
    parser_x_modes=(ParseMode.proximity_diag,),
    parser_y_modes=(ParseMode.proximity_diag,))

previous_v1_params4[308] = proto.update(
    parser_x_modes=(ParseMode.color_proximity_diag,),
    parser_y_modes=(ParseMode.color_proximity_diag,))

previous_v1_params4[310] = proto.update(
    parser_x_modes=(ParseMode.crop,),
    parser_y_modes=(ParseMode.crop,))

previous_v1_params4[311] = proto.update(
    parser_x_modes=(ParseMode.crop,),
    parser_y_modes=(ParseMode.crop,), enable_free_draw=True)

previous_v1_params4[314] = proto.update(
    parser_x_modes=(ParseMode.crop,),
    parser_y_modes=(ParseMode.crop,), enable_free_draw=True)

previous_v1_params4[316] = proto.update(
    parser_x_modes=(ParseMode.crop,),
    parser_y_modes=(ParseMode.crop,))

previous_v1_params4[317] = proto.update(
    parser_x_modes=(ParseMode.partition,),
    parser_y_modes=(ParseMode.crop,))

previous_v1_params4[319] = proto.update(
    parser_x_modes=(ParseMode.proximity_diag,),
    parser_y_modes=(ParseMode.proximity_diag,))

previous_v1_params4[321] = proto.update(
    parser_x_modes=(ParseMode.proximity_diag,),
    parser_y_modes=(ParseMode.crop,))

previous_v1_params4[325] = proto.update(
    parser_x_modes=(ParseMode.proximity_diag,),
    parser_y_modes=(ParseMode.proximity_diag,))

previous_v1_params4[328] = proto.update(
    parser_x_modes=(ParseMode.crop,),
    parser_y_modes=(ParseMode.crop,), enable_free_draw=True)

previous_v1_params4[329] = proto.update(
    parser_x_modes=(ParseMode.color_proximity_diag,),
    parser_y_modes=(ParseMode.color_proximity_diag,))

previous_v1_params4[330] = proto.update(
    parser_x_modes=(ParseMode.crop,),
    parser_y_modes=(ParseMode.crop,), enable_free_draw=True)

previous_v1_params4[333] = proto.update(
    parser_x_modes=(ParseMode.crop,),
    parser_y_modes=(ParseMode.crop,))

previous_v1_params4[336] = proto.update(
    parser_x_modes=(ParseMode.color_proximity_diag,),
    parser_y_modes=(ParseMode.color_proximity_diag,))

previous_v1_params4[338] = proto.update(
    parser_x_modes=(ParseMode.proximity_diag,),
    parser_y_modes=(ParseMode.proximity_diag,))

previous_v1_params4[342] = proto.update(
    parser_x_modes=(ParseMode.proximity_diag,),
    parser_y_modes=(ParseMode.proximity_diag,), enable_free_draw=True)

previous_v1_params4[343] = proto.update(
    parser_x_modes=(ParseMode.crop,),
    parser_y_modes=(ParseMode.crop,), enable_free_draw=True)

previous_v1_params4[346] = proto.update(
    parser_x_modes=(ParseMode.partition_by_size,),
    parser_y_modes=(ParseMode.crop,))

previous_v1_params4[350] = proto.update(
    parser_x_modes=(ParseMode.crop,),
    parser_y_modes=(ParseMode.crop,))

previous_v1_params4[351] = proto.update(
    parser_x_modes=(ParseMode.crop,),
    parser_y_modes=(ParseMode.crop,))

previous_v1_params4[352] = proto.update(
    parser_x_modes=(ParseMode.proximity_diag,),
    parser_y_modes=(ParseMode.color_proximity_diag,))

previous_v1_params4[353] = proto.update(
    parser_x_modes=(ParseMode.proximity_normal,),
    parser_y_modes=(ParseMode.proximity_normal,))

previous_v1_params4[355] = proto.update(
    parser_x_modes=(ParseMode.crop,),
    parser_y_modes=(ParseMode.crop,))

previous_v1_params4[357] = proto.update(
    parser_x_modes=(ParseMode.crop,),
    parser_y_modes=(ParseMode.proximity_diag,))

previous_v1_params4[358] = proto.update(
    parser_x_modes=(ParseMode.crop,),
    parser_y_modes=(ParseMode.crop,), enable_free_draw=True)

previous_v1_params4[360] = proto.update(
    parser_x_modes=(ParseMode.proximity_diag,),
    parser_y_modes=(ParseMode.proximity_diag,))

previous_v1_params4[361] = proto.update(
    parser_x_modes=(ParseMode.color_proximity_diag,),
    parser_y_modes=(ParseMode.color_proximity_diag,), enable_stack=True)

previous_v1_params4[368] = proto.update(
    parser_x_modes=(ParseMode.proximity_normal,),
    parser_y_modes=(ParseMode.proximity_normal,))

previous_v1_params4[371] = proto.update(
    parser_x_modes=(ParseMode.partition,),
    parser_y_modes=(ParseMode.crop,), enable_split=True)

previous_v1_params4[372] = proto.update(
    parser_x_modes=(ParseMode.crop,),
    parser_y_modes=(ParseMode.crop,), enable_free_draw=True)

previous_v1_params4[373] = proto.update(
    parser_x_modes=(ParseMode.color_proximity_diag,),
    parser_y_modes=(ParseMode.color_proximity_diag,))

previous_v1_params4[374] = proto.update(
    parser_x_modes=(ParseMode.crop,),
    parser_y_modes=(ParseMode.proximity_diag,))

previous_v1_params4[379] = proto.update(
    parser_x_modes=(ParseMode.crop,),
    parser_y_modes=(ParseMode.crop,))

previous_v1_params4[384] = proto.update(
    parser_x_modes=(ParseMode.proximity_diag,),
    parser_y_modes=(ParseMode.proximity_diag,))

previous_v1_params4[385] = proto.update(
    parser_x_modes=(ParseMode.partition,),
    parser_y_modes=(ParseMode.crop,))

previous_v1_params4[388] = proto.update(
    parser_x_modes=(ParseMode.crop,),
    parser_y_modes=(ParseMode.crop,), enable_free_draw=True)

previous_v1_params4[393] = proto.update(
    parser_x_modes=(ParseMode.crop,),
    parser_y_modes=(ParseMode.crop,))

previous_v1_params4[394] = proto.update(
    parser_x_modes=(ParseMode.partition_by_size,),
    parser_y_modes=(ParseMode.crop,))

previous_v1_params4[399] = proto.update(
    parser_x_modes=(ParseMode.crop,),
    parser_y_modes=(ParseMode.crop,))
